spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver

  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: create-drop

  liquibase:
    enabled: false